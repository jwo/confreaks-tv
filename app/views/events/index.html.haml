.row
  .span12
    %h1
      Events
.row
  -k=-1
  -@events.each do |event|
    -k+=1
    .span6
      .well
        .conference-mini-display-outer
          .conference-mini-display
            .inner
              = link_to image_tag(event.logo.url), event_path(event)
              .caption
                = link_to event.display_name, event_path(event)
    -if (k+1) % 2 == 0
      .clearfix
